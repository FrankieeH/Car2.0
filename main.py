from car import Car
from parkinglot import ParkingLot

import string
import logging
import random


logging.basicConfig(
	level= logging.DEBUG,
	format= '{asctime} {filename} {levelno} {message}',
	style= '{'
)